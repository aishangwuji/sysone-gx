# sysone.originagent.cn HTTP 入口：ACME 验证 + 301 跳转 HTTPS
server {
    listen 80;
    server_name sysone.originagent.cn;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
